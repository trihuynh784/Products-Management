mixin sub-menu(path, items)
  ul
    each item in items
      li 
        a(href=`${path}/${item.slug}`) #{item.title}
        if (item.children && item.children.length > 0) 
          +sub-menu(path, item.children)